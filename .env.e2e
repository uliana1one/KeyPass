# E2E Test Environment Configuration
# Copy this file to .env.e2e and fill in your test values

# Required: Private key for test wallet (must have DEV tokens on Moonbase Alpha)
# Get testnet DEV tokens from: https://faucet.moonbeam.network/
TEST_PRIVATE_KEY=0x1234567890123456789012345678901234567890123456789012345678901234

# Optional: Recipient address for minting tests (defaults to test wallet address)
TEST_RECIPIENT=0x1234567890123456789012345678901234567890

# Optional: Test timeout in milliseconds (default: 300000 = 5 minutes)
TEST_TIMEOUT=300000

# Optional: RPC endpoint (default: Moonbase Alpha)
TEST_RPC_URL=https://rpc.api.moonbase.moonbeam.network

# Optional: Chain ID (default: 1287 for Moonbase Alpha)
TEST_CHAIN_ID=1287

# Optional: Minimum wallet balance in DEV (default: 0.1)
TEST_MIN_BALANCE=0.1

# Optional: Enable verbose logging (default: true)
TEST_VERBOSE=true

# Optional: Test data cleanup (default: true)
TEST_CLEANUP=true

# Optional: Parallel test execution (default: false for E2E tests)
TEST_PARALLEL=false

# Optional: Retry configuration
TEST_MAX_RETRIES=3
TEST_RETRY_DELAY=1000

# Optional: Gas configuration
TEST_GAS_LIMIT=500000
TEST_GAS_PRICE_GWEI=1

# Optional: Contract verification
TEST_VERIFY_CONTRACTS=true
TEST_VERIFICATION_DELAY=30000

# Optional: IPFS configuration for metadata
TEST_IPFS_GATEWAY=https://ipfs.io/ipfs/
TEST_IPFS_API_URL=your_ipfs_api_url_here
TEST_IPFS_API_KEY=your_ipfs_api_key_here